using System;

namespace OllamaApi.Helpers
{
    /* public class AuditLogger
     {
         private static readonly AuditLogger _instance = new AuditLogger();
         public static AuditLogger Instance => _instance;

         public void Log(string message, string content)
         {
             Console.WriteLine($"[SECURITY LOG] {DateTime.UtcNow:u} | {message}");
             Console.WriteLine($"→ Content: {content}\n");
         }
     }*/
}
